# 🎓 University Schedule Management System

<div align="center">

![TypeScript](https://img.shields.io/badge/TypeScript-5.3-blue?logo=typescript)
![Node.js](https://img.shields.io/badge/Node.js-16+-green?logo=node.js)
![License](https://img.shields.io/badge/License-MIT-yellow)
![Status](https://img.shields.io/badge/Status-Completed-success)

**Система управління розкладом занять в університеті**

Проект демонструє використання **Type Aliases**, **Union Types** та **Arrays** в TypeScript для створення повнофункціональної системи управління розкладом.

[Особливості](#особливості) • [Встановлення](#встановлення) • [Використання](#використання) • [Документація](#документація)

</div>

```
## 🚀 Швидкий старт

### Варіант 1: Запуск через ts-node (рекомендовано для розробки)
```bash
npm run dev
```

### Варіант 2: Компіляція та запуск
```bash
# Компілювати TypeScript в JavaScript
npm run build

# Запустити скомпільований код
npm start
```

### Варіант 3: Все в одній команді
```bash
npm start
```

---

## 📋 Зміст

- [Особливості](#особливості)
- [Технології](#технології)
- [Встановлення](#встановлення)
- [Швидкий старт](#швидкий-старт)
- [Структура проекту](#структура-проекту)
- [Що відбувається при запуску](#що-відбувається-при-запуску)
- [API Документація](#api-документація)
- [Приклади використання](#приклади-використання)
- [Ліцензія](#ліцензія)

---

## ✨ Особливості

### ✅ Реалізовані функції:

- 📚 **Управління професорами** - додавання та перегляд розкладу викладачів
- 🏫 **Управління аудиторіями** - відстеження вільних приміщень
- 📖 **Управління курсами** - різні типи занять (лекції, практики, лабораторні)
- 📅 **Розклад занять** - створення та модифікація розкладу
- ⚠️ **Автоматична валідація** - перевірка конфліктів професорів та аудиторій
- 📊 **Аналітика** - статистика використання ресурсів
- 🔄 **Модифікація** - зміна аудиторій та скасування занять

### 🎯 Технічні особливості:

- ✅ Повна типізація TypeScript без використання `any`
- ✅ Type Aliases замість Interfaces
- ✅ Union Types для обмежених значень
- ✅ Модульна архітектура
- ✅ JSDoc коментарі
- ✅ Обробка помилок та крайових випадків

---

## 🛠 Технології

- **TypeScript 5.3+** - строга типізація
- **Node.js 16+** - виконання коду
- **ts-node** - запуск TypeScript без компіляції

---

## 📦 Встановлення

### Передумови

Переконайтеся, що у вас встановлено:

- [Node.js](https://nodejs.org/) версії 16 або вище
- npm (встановлюється разом з Node.js)

### Кроки встановлення
```bash
# 1. Клонуйте репозиторій
git clone https://github.com/your-username/university-schedule-system.git
cd university-schedule-system

# 2. Встановіть залежності
npm install

# 3. Готово! Тепер можна запускати проект
```

---

## 📁 Структура проекту
```
university-schedule-system/
├── src/
│   ├── types/                      # Визначення типів
│   │   ├── basic.types.ts          # DayOfWeek, TimeSlot, CourseType
│   │   ├── entities.types.ts       # Professor, Classroom, Course, Lesson
│   │   ├── conflict.types.ts       # ScheduleConflict
│   │   └── index.ts                # Експорт типів
│   │
│   ├── data/
│   │   └── storage.ts              # Клас для зберігання даних
│   │
│   ├── validators/
│   │   └── schedule.validator.ts   # Валідація конфліктів
│   │
│   ├── services/                   # Бізнес-логіка
│   │   ├── professor.service.ts    # Робота з професорами
│   │   ├── classroom.service.ts    # Робота з аудиторіями
│   │   ├── lesson.service.ts       # Робота з заняттями
│   │   └── analytics.service.ts    # Аналітика та звіти
│   │
│   └── index.ts                    # Головний файл експорту
│
├── examples/
│   └── demo.ts                     # Демонстрація системи
│
├── dist/                           # Скомпільовані файли (генерується)
├── node_modules/                   # Залежності
├── .gitignore
├── package.json
├── tsconfig.json
└── README.md
```

---

## 🎬 Що відбувається при запуску

Коли ви запускаєте `npm run dev` або `npm start`, виконується демонстраційний скрипт, який показує всі можливості системи:

### 1️⃣ Додавання професорів
```
📚 ДОДАВАННЯ ПРОФЕСОРІВ
─────────────────────────────────────────────────────────────
✓ Додано 3 професорів
  Всього професорів: 3
```

Система додає трьох професорів:
- Іванов Іван Іванович (Інформатика)
- Петренко Петро Петрович (Математика)
- Сидоренко Сидір Сидорович (Фізика)

### 2️⃣ Додавання аудиторій
```
🏫 ДОДАВАННЯ АУДИТОРІЙ
─────────────────────────────────────────────────────────────
✓ Додано 4 аудиторії
  Всього аудиторій: 4
```

Створюються 4 аудиторії з різною місткістю та обладнанням.

### 3️⃣ Додавання курсів
```
📖 ДОДАВАННЯ КУРСІВ
─────────────────────────────────────────────────────────────
✓ Додано 5 курсів
  Всього курсів: 5
```

Реєструються курси: Програмування, Алгоритми, Бази даних, Веб-розробка, Тестування ПЗ.

### 4️⃣ Створення розкладу
```
📅 ДОДАВАННЯ ЗАНЯТЬ ДО РОЗКЛАДУ
─────────────────────────────────────────────────────────────
  ✓ Заняття 1: Додано
  ✓ Заняття 2: Додано
  ✓ Заняття 3: Додано
  ✓ Заняття 4: Додано
  ✓ Заняття 5: Додано

  Всього занять у розкладі: 5
```

Система додає заняття в розклад з автоматичною перевіркою конфліктів.

### 5️⃣ Перевірка конфліктів
```
⚠️  ПЕРЕВІРКА КОНФЛІКТІВ
─────────────────────────────────────────────────────────────
  Спроба додати заняття з конфліктом професора...
  ✗ Результат: Відхилено (конфлікт)
```

Демонструється як система запобігає конфліктам (професор не може вести дві пари одночасно).

### 6️⃣ Пошук вільних аудиторій
```
🔍 ПОШУК ВІЛЬНИХ АУДИТОРІЙ
─────────────────────────────────────────────────────────────
  Понеділок, 8:30-10:00
  Вільні аудиторії: 102, 301
```

Система показує які аудиторії доступні в конкретний час.

### 7️⃣ Розклад професора
```
👨‍🏫 РОЗКЛАД ПРОФЕСОРА
─────────────────────────────────────────────────────────────
  Професор #1 (Іванов І.І.)
  Кількість занять: 2
    1. Monday, 8:30-10:00, ауд. 101
    2. Monday, 10:15-11:45, ауд. 102
```

Виводиться повний розклад конкретного викладача.

### 8️⃣ Аналітика
```
📊 АНАЛІТИКА
─────────────────────────────────────────────────────────────
  Використання аудиторії 101: 8.0%
  Використання аудиторії 102: 4.0%
  Найпопулярніший тип занять: Lecture
```

Система аналізує використання ресурсів університету.

### 9️⃣ Модифікація розкладу
```
✏️  МОДИФІКАЦІЯ РОЗКЛАДУ
─────────────────────────────────────────────────────────────
  Зміна аудиторії для заняття #0...
  ✓ Результат: Успішно змінено

  Скасування заняття #2...
  Занять до скасування: 5
  Занять після скасування: 4
```

Демонструється можливість редагування розкладу.

### 🔟 Фінальна статистика
```
╔════════════════════════════════════════════════════════════╗
║   ДЕМОНСТРАЦІЯ ЗАВЕРШЕНА                                   ║
╚════════════════════════════════════════════════════════════╝

📈 Фінальна статистика:
   • Професорів: 3
   • Аудиторій: 4
   • Курсів: 5
   • Занять у розкладі: 4
```

---

## 📚 API Документація

### Типи даних

#### Базові типи
```typescript
// Дні тижня
type DayOfWeek = "Monday" | "Tuesday" | "Wednesday" | "Thursday" | "Friday";

// Часові слоти
type TimeSlot = "8:30-10:00" | "10:15-11:45" | "12:15-13:45" | "14:00-15:30" | "15:45-17:15";

// Типи занять
type CourseType = "Lecture" | "Seminar" | "Lab" | "Practice";
```

#### Основні сутності
```typescript
type Professor = {
  id: number;
  name: string;
  department: string;
};

type Classroom = {
  number: string;
  capacity: number;
  hasProjector: boolean;
};

type Course = {
  id: number;
  name: string;
  type: CourseType;
};

type Lesson = {
  courseId: number;
  professorId: number;
  classroomNumber: string;
  dayOfWeek: DayOfWeek;
  timeSlot: TimeSlot;
};
```

### Основні функції

#### Управління професорами

**`addProfessor(professor: Professor): void`**

Додає нового професора до системи.
```typescript
addProfessor({
  id: 1,
  name: "Іванов І.І.",
  department: "Інформатика"
});
```

**`getProfessorSchedule(professorId: number): Lesson[]`**

Повертає всі заняття конкретного професора.
```typescript
const schedule = getProfessorSchedule(1);
console.log(`Професор має ${schedule.length} занять`);
```

#### Управління аудиторіями

**`findAvailableClassrooms(timeSlot: TimeSlot, dayOfWeek: DayOfWeek): string[]`**

Знаходить вільні аудиторії у вказаний час.
```typescript
const available = findAvailableClassrooms("8:30-10:00", "Monday");
console.log("Вільні аудиторії:", available);
```

**`getClassroomUtilization(classroomNumber: string): number`**

Розраховує відсоток використання аудиторії (0-100%).
```typescript
const usage = getClassroomUtilization("101");
console.log(`Аудиторія використовується на ${usage.toFixed(1)}%`);
```

#### Управління заняттями

**`addLesson(lesson: Lesson): boolean`**

Додає заняття до розкладу. Повертає `true` якщо успішно, `false` якщо є конфлікт.
```typescript
const success = addLesson({
  courseId: 1,
  professorId: 1,
  classroomNumber: "101",
  dayOfWeek: "Monday",
  timeSlot: "8:30-10:00"
});

if (success) {
  console.log("Заняття додано!");
} else {
  console.log("Конфлікт розкладу!");
}
```

**`reassignClassroom(lessonIndex: number, newClassroomNumber: string): boolean`**

Змінює аудиторію для існуючого заняття.
```typescript
const changed = reassignClassroom(0, "102");
```

**`cancelLesson(lessonIndex: number): void`**

Видаляє заняття з розкладу.
```typescript
cancelLesson(2);
```

#### Валідація

**`validateLesson(lesson: Lesson): ScheduleConflict | null`**

Перевіряє чи не створює заняття конфліктів. Повертає `null` якщо конфліктів немає.
```typescript
const conflict = validateLesson(newLesson);
if (conflict) {
  console.log(`Конфлікт: ${conflict.type}`);
}
```

#### Аналітика

**`getMostPopularCourseType(): CourseType`**

Визначає найпопулярніший тип занять в розкладі.
```typescript
const popular = getMostPopularCourseType();
console.log(`Найбільше в розкладі: ${popular}`);
```

---

## 💡 Приклади використання

### Приклад 1: Створення простого розкладу
```typescript
import { addProfessor, addLesson, storage } from './src';

// Додати професора
addProfessor({
  id: 1,
  name: "Шевченко Т.Г.",
  department: "Література"
});

// Додати аудиторію
storage.addClassroom({
  number: "305",
  capacity: 40,
  hasProjector: true
});

// Додати курс
storage.addCourse({
  id: 1,
  name: "Українська література",
  type: "Lecture"
});

// Створити заняття
const success = addLesson({
  courseId: 1,
  professorId: 1,
  classroomNumber: "305",
  dayOfWeek: "Wednesday",
  timeSlot: "10:15-11:45"
});

console.log(success ? "✓ Заняття створено" : "✗ Помилка");
```

### Приклад 2: Перевірка доступності аудиторій
```typescript
import { findAvailableClassrooms, addLesson } from './src';

// Перевірити вільні аудиторії
const free = findAvailableClassrooms("14:00-15:30", "Friday");

if (free.length > 0) {
  console.log(`Доступні аудиторії: ${free.join(", ")}`);
  
  // Забронювати першу вільну
  addLesson({
    courseId: 5,
    professorId: 2,
    classroomNumber: free[0],
    dayOfWeek: "Friday",
    timeSlot: "14:00-15:30"
  });
}
```

### Приклад 3: Аналіз завантаженості
```typescript
import { getClassroomUtilization, storage } from './src';

// Перевірити всі аудиторії
const classrooms = storage.getClassrooms();

console.log("Завантаженість аудиторій:");
classrooms.forEach(room => {
  const usage = getClassroomUtilization(room.number);
  console.log(`  ${room.number}: ${usage.toFixed(1)}%`);
});
```


## 🤝 Внесок

Якщо ви хочете покращити проект:

1. Fork репозиторій
2. Створіть feature branch (`git checkout -b feature/AmazingFeature`)
3. Commit зміни (`git commit -m 'Add some AmazingFeature'`)
4. Push в branch (`git push origin feature/AmazingFeature`)
5. Відкрийте Pull Request

---

## 📄 Ліцензія

Цей проект розповсюджується під ліцензією MIT. Дивіться файл `LICENSE` для детальної інформації.

---

## 👨‍💻 Автор

**Your Name**

- GitHub: https://github.com/Sentore847
- Email: grigorchuk.dev@gmai.com

---

## 🙏 Подяки

Цей проект був створений як навчальне завдання для демонстрації можливостей TypeScript, зокрема:
- Type Aliases
- Union Types
- Arrays та їх методи
- Модульної архітектури

---

<div align="center">

**⭐ Якщо проект вам сподобався, поставте зірочку! ⭐**

Made with ❤️ and TypeScript

</div>
